<resources>

    <!-- Base application theme. -->
    <style name="AppTheme"
        parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorAccent">#009688</item>
        <!-- Force the action bar to the brand purple in BOTH light and dark mode.
             Without this, the dark theme paints the action bar with the dark surface
             colour (near-black). -->
        <item name="actionBarStyle">@style/AppActionBar</item>
    </style>

    <style name="AppActionBar" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="background">@color/colorPrimary</item>
    </style>

</resources>
